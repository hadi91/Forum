
<%= form_for ([@topic,@topic.comments.build]) do |f| %>
  <p>
    <%= f.label :content, "Comment" %><br>
    <%= f.text_area :body, :class => 'commentbody' %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>
